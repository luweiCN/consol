# ConSol VS Code Extension

Future VS Code extension for ConSol.

This is intentionally deferred. The extension should act as a thin UI client over the `consol` CLI/protocol.

Expected future responsibilities:

- command palette actions
- context menu deploy/call/send
- state and event panels
- diagnostics from structured CLI output

